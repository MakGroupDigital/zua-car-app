rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Copie dev permissive alignée avec /firestore.rules.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
