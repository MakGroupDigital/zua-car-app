rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Règles permissives TEMPORAIRES pour développement.
    // Tout utilisateur connecté peut lire et écrire tous les fichiers.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
